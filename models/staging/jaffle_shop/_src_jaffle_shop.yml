# {{ codegen.generate_source(schema_name= 'jaffle_shop', database_name= 'raw') }} - For generating sources

version: 2

sources:
  - name: jaffle_shop
    database: raw 
    schema: jaffle_shop
    config: 
      freshness:
        warn_after:
          count: 7
          period: day
        error_after:
          count: 10
          period: day
      loaded_at_field: _etl_loaded_at
    tables:
      - name: customers
        config:
          freshness : null
      - name: orders