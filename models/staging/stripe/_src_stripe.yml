# {{ codegen.generate_source(schema_name= 'stripe', database_name= 'raw') }} - For generating sources

version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        config:
          loaded_at_field: _batched_at
          freshness:
            warn_after: {count: 30, period: day}
            error_after: {count: 60, period: day}
